# Phase 3 Review Fixes

- [x] Resolve a cancelled append-retry backoff so the stale async request can complete safely.
- [x] Consolidate History loading and count announcements into one polite live region.
